{
  "name": "coffee-lex",
  "description": "Stupid lexer for CoffeeScript.",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "check-format": "prettier-check --parser typescript --print-width 120 --single-quote '{src,test}/**/*.ts'",
    "reformat": "prettier --parser typescript --print-width 120 --single-quote --write '{src,test}/**/*.ts'",
    "prelint": "yarn run check-format",
    "lint": "tslint --config tslint.json --project tsconfig.json --type-check",
    "prelint-fix": "yarn run reformat",
    "lint-fix": "tslint --config tslint.json --project tsconfig.json --type-check --fix",
    "pretest": "yarn run lint",
    "test": "mocha"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/decaffeinate/coffee-lex.git"
  },
  "keywords": [
    "coffee",
    "coffeescript",
    "lex",
    "parse"
  ],
  "files": [
    "dist"
  ],
  "author": "Brian Donovan <me@brian-donovan.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/decaffeinate/coffee-lex/issues"
  },
  "homepage": "https://github.com/decaffeinate/coffee-lex",
  "devDependencies": {
    "@types/mocha": "^2.2.41",
    "@types/node": "^8.0.9",
    "decaffeinate-coffeescript": "^1.10.0-patch25",
    "mocha": "^4.0.0",
    "prettier": "1.6.1",
    "prettier-check": "2.0.0",
    "prettier": "1.7.2",
    "prettier-check": "2.0.0",
    "semantic-release": "^6.3.5",
    "ts-node": "^3.2.0",
    "tslint": "^5.5.0",
    "typescript": "^2.4.1"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
